{
  "$schema": "./deployment-config.schema.json",
  "targetEnvironment": "gamma",
  "_comment": "Valid values: 'alpha', 'gamma', 'prod'. Default is 'gamma'. To deploy to a different environment, change targetEnvironment and submit a PR."
}
